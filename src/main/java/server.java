//receives connections from phones, hands them off to processor threads.
public class server {

    //creates ports, receives inputs from phones
    public static void receive_call(int port) {

    }
}
